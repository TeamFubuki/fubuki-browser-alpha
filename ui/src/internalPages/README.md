# Internal Pages

Internal pages are still generated by native CEF scheme handling in
`native/src/cef/FubukiSchemeHandler.cc`.

Use this folder when an internal page is moved into the SolidJS UI shell. Keep route-level page
components small and avoid coupling them directly to raw bridge method strings.
